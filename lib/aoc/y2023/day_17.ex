defmodule AOC.Y2023.Day17 do
  @moduledoc false
  use AOC.Solution, year: 2023, day: 17

  @impl true
  def load_data(data, _opts) do
    # Data.load_day_as_grid(2023, 17)
    # |> then(fn {grid, m, n} ->
    #   {Map.new(grid, fn {k, v} -> {k, String.to_integer(v)} end), m, n}
    # end)
  end

  @impl true
  def part_one(_data, _opts) do
  end

  @impl true
  def part_two(_data, _opts) do
  end

  # def walk(grid, m, n, frontier) do

  # end
end
